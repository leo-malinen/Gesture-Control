__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
build/
dist/

# The MediaPipe hand model is fetched once with `airwave fetch-model`.
models/*.task

# Recorded gestures and trained models are personal; they should not be
# committed by accident, but the directory layout should survive a clone.
data/gestures/*.npz
data/gestures/manifest.json
data/*.npz

.DS_Store
Thumbs.db
